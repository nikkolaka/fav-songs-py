# Shared environment template for both:
# - local `docker compose`
# - Portainer "Load variables from .env file"

# Container image used by compose.portainer.yml
FAVSONGS_IMAGE=ghcr.io/your-github-user/fav-songs-py:latest

# Host port mapped to the container
APP_PORT=8090

# Spotify application credentials
CLIENT_ID=
CLIENT_SECRET=

# Must exactly match the redirect URI configured in Spotify
# Example: http://server.local:8090/api/auth/callback
REDIRECT_URI=

# Optional overrides
SPOTIFY_SCOPE=user-read-playback-state,user-read-currently-playing,playlist-modify-public,playlist-modify-private
DEFAULT_PLAYLIST_NAME=Favourite Songs - Whatsit
FAVSONGS_DATA_DIR=/app/data
FAVSONGS_DB_PATH=/app/data/favsongs.db
